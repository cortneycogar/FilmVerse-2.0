<!DOCTYPE html>
<html>
  <head>
    <title>List of Films</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <a href="/"><h1>Film Data</h1></a>
    <% if (items.length > 0) { %>
    <ul>
      <% items.forEach(function(item) { %>
      <li><a href="/films/<%=item._id%>_<%=item.year%>"><%=item.title%></a></li>
      <% }); %>
    </ul>
    <% } else { %>
    <div>No films found.</div>
    <% } %>
    <div class="control">
      <h2>Search for films by name</h2>
      <form method="get" action="/films/">
        <label>Film name: <input name="name"></label>
        <button>Search</button>
      </form>
    </div>

<div id="new-film">
    <form method="post" action="/add-film/">
    <label> Title <input type="text" name="film[title]" required value="" /></label>
	<label> Year <input type="text" name="film[year]" required value="" /></label>
	<label> Director <select type="text" name="film[director]">
            <% directors.forEach(function(director) { %>
            <option value="<%=related_item._id%>">
              <%=related_item.name%>
            </option>
            <% }); %>
    </select></label>
	<label> Producer <input type="text" name="film[producer]" value="" /></label>
	<label> Writer <input type="text" name="film[writer]" value="" /></label>
	<label> Distributor <input type="text" name="film[distributor]" value="" /></label>
	<label> Rating <input type="text" name="film[rating]" value="" /></label>
	<label> Genre <input type="text" name="film[genre]" value="" /></label>
	<label> Language <input type="text" name="film[language]" value="" /></label>
	<label> Actor <textarea name="film[actor]"></textarea></label>
<label> Description <textarea name="film[description]"></textarea></label>
	<input type="submit" /></label>
	</form>
</div>
  </body>
</html>
